import s from '../HomeView/HomeView.module.css';

const HomeView = () => (
  <div className={s.container}>
    <h1 className={s.title}>Приветственная страница нашего сервиса</h1>
  </div>
);

export default HomeView;
